import { GlobalStyles } from '@mui/material';

const themeGlobalCss = (
    <GlobalStyles
        styles={{
            body: {
                color: 'black',
            },
        }}
    />
);

export default themeGlobalCss;
